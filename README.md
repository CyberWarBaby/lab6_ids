# lab6_ids
AI-Based Anomaly and Intrusion Detection System using the CIC UNSW-NB15 dataset. Implements unsupervised models (Isolation Forest, Autoencoder) and a supervised Random Forest classifier to detect network intrusions, generate ROC curves, and visualize anomaly scores. Includes comparison of model performance and outputs summary tables and plots.
